services:
  api:
    build: ./api
    ports:
      - "1323:1323"

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - api

  reverse-proxy:
    build: ./nginx
    ports:
      - "443:443"
    depends_on:
      - api
      - frontend
