post:
  type: object
  properties:
    id:
      type: string
    user:
      $ref: '#/post-user'
    content:
      type: string
    like_count:
      type: integer
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
  required:
    - id
    - user
    - content
    - like-count
    - created_at
    - updated_at

post-user:
  type: object
  properties:
    username:
      type: string
    protocol:
      type: string
    display_name:
      type: string
    icon:
      type: string
  required:
    - username
    - protocol
    - display_name
    - icon

newpost:
  type: object
  properties:
    content:
      type: string
  required:
    - content
    