post:
  type: object
  properties:
    id:
      type: string
    user:
      $ref: './user.yaml#/simple-user'
    content:
      type: string
    like_count:
      type: integer
    created_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
  required:
    - id
    - user
    - content
    - like-count
    - created_at
    - updated_at

newpost:
  type: object
  properties:
    content:
      type: string
  required:
    - content
    