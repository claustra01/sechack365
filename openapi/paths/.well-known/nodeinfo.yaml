get:
  tags:
    - .well-known
  description: NodeInfo
  responses:
    '200':
      description: NodeInfo URL
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/.well-known.yaml#/wellknown-nodeinfo'
